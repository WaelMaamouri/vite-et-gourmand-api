parameters:
    cloudinary.url: "%env(CLOUDINARY_URL)%"

services:
    App\Service\CloudinaryService:
        autowire: false
        arguments:
            - "%cloudinary.url%"
            - "@logger"

    App\Service\CloudinaryUrlHelper:
        autowire: false
        arguments:
            - "%cloudinary.url%"
