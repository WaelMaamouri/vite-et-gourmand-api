framework:
    mailer:
        # Brevo : login avec @ → %40 ; mot de passe = variable BREVO_SMTP_KEY (Render / .env.local)
        dsn: "smtp://a296d2001%40smtp-brevo.com:%env(BREVO_SMTP_KEY)%@smtp-relay.brevo.com:587"
